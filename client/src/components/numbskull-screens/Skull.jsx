// The feature screens reuse the single app mascot (the premium robot) so there's
// one consistent character everywhere. Same API: expression / size / glow.
export { default } from '../skull/SkullMascot.jsx'
